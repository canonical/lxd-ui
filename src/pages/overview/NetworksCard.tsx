import type { FC } from "react";
import { Card } from "@canonical/react-components";

const NetworksCard: FC = () => {
  return <Card className="overview-card networks"></Card>;
};

export default NetworksCard;
